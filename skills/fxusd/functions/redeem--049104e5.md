# redeem

**Signature**: `redeem(uint256 fTokenIn,uint256 xTokenIn,address owner)`

**Selector**: `0x049104e5`

**Mutability**: nonpayable

**Contract**: `FxTreasuryV2` (fx Protocol)

## Inputs
- `fTokenIn` (`uint256`): TODO(curator): describe
- `xTokenIn` (`uint256`): TODO(curator): describe
- `owner` (`address`): TODO(curator): describe

## Outputs
- `baseOut` (`uint256`): TODO(curator): describe

## What it does

Burn `amount` of fxUSD; receive `baseToken` from the market. `minOut` caps slippage. May incur a redemption fee. See <https://docs.aladdin.club/fx-protocol/>.

## Security notes

Redemption charges a per-market fee. Surface the fee from `MarketV2.fTokenRedemptionFeeRatio()` / `MarketV2.xTokenRedemptionFeeRatio()` before signing.

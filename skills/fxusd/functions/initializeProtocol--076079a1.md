# initializeProtocol

**Signature**: `initializeProtocol(uint256 baseIn)`

**Selector**: `0x076079a1`

**Mutability**: nonpayable

**Contract**: `FxTreasuryV2` (fx Protocol)

## Inputs
- `baseIn` (`uint256`): TODO(curator): describe

## Outputs
- `fTokenOut` (`uint256`): TODO(curator): describe
- `xTokenOut` (`uint256`): TODO(curator): describe

## What it does

TODO(curator): operational semantics for `FxTreasuryV2.initializeProtocol` — see <https://docs.aladdin.club/fx-protocol/>.

## Security notes

TODO(curator): permission boundary, oracle dependency, slippage / collateral-ratio implications.

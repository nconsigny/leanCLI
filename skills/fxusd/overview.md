# fxusd — overview

TODO(curator): protocol summary, what it is, primary user-facing flows.

This stub was generated by Phase 1b scaffolding. See `contracts.json` for the
deployment addresses and `abi/` for the contract ABIs. Function-by-function
summaries live in `functions/`.

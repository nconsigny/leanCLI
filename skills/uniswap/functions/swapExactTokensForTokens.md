# swapExactTokensForTokens

**Signature**: `swapExactTokensForTokens(uint256 amountIn,uint256 amountOutMin,address[] path,address to)`

**Selector**: `0x472b43f3`

**Mutability**: payable

**Contract**: `SwapRouter02` (Uniswap)

## Inputs
- `amountIn` (`uint256`): TODO(curator): describe
- `amountOutMin` (`uint256`): TODO(curator): describe
- `path` (`address[]`): TODO(curator): describe
- `to` (`address`): TODO(curator): describe

## Outputs
- `amountOut` (`uint256`): TODO(curator): describe

## What it does

TODO(curator): operational semantics for `SwapRouter02.swapExactTokensForTokens` — see <https://docs.uniswap.org/contracts/v3/reference/periphery/SwapRouter>.

## Security notes

TODO(curator): approval surface, slippage, recipient checks, callbacks, multicall framing.

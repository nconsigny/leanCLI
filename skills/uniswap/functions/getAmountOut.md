# getAmountOut

**Signature**: `getAmountOut(uint256 amountIn,uint256 reserveIn,uint256 reserveOut)`

**Selector**: `0x054d50d4`

**Mutability**: pure

**Contract**: `UniswapV2Router02` (Uniswap)

## Inputs
- `amountIn` (`uint256`): TODO(curator): describe
- `reserveIn` (`uint256`): TODO(curator): describe
- `reserveOut` (`uint256`): TODO(curator): describe

## Outputs
- `amountOut` (`uint256`): TODO(curator): describe

## What it does

TODO(curator): operational semantics for `UniswapV2Router02.getAmountOut` — see <https://docs.uniswap.org/contracts/v2/reference/smart-contracts/router-02>.

## Security notes

TODO(curator): approval surface, slippage, recipient checks, callbacks, multicall framing.

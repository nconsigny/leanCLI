# exactOutput

**Signature**: `exactOutput((bytes path,address recipient,uint256 amountOut,uint256 amountInMaximum) params)`

**Selector**: `0x09b81346`

**Mutability**: payable

**Contract**: `SwapRouter02` (Uniswap)

## Inputs
- `params` (`(bytes path,address recipient,uint256 amountOut,uint256 amountInMaximum)`): TODO(curator): describe

## Outputs
- `amountIn` (`uint256`): TODO(curator): describe

## What it does

TODO(curator): operational semantics for `SwapRouter02.exactOutput` — see <https://docs.uniswap.org/contracts/v3/reference/periphery/SwapRouter>.

## Security notes

TODO(curator): approval surface, slippage, recipient checks, callbacks, multicall framing.

# onERC1155BatchReceived

**Signature**: `onERC1155BatchReceived(address,address,uint256[],uint256[],bytes)`

**Selector**: `0xbc197c81`

**Mutability**: pure

**Contract**: `UniversalRouter` (Uniswap)

## Inputs
- `(unnamed)` (`address`): TODO(curator): describe
- `(unnamed)` (`address`): TODO(curator): describe
- `(unnamed)` (`uint256[]`): TODO(curator): describe
- `(unnamed)` (`uint256[]`): TODO(curator): describe
- `(unnamed)` (`bytes`): TODO(curator): describe

## Outputs
- `(unnamed)` (`bytes4`): TODO(curator): describe

## What it does

TODO(curator): operational semantics for `UniversalRouter.onERC1155BatchReceived` — see <https://docs.uniswap.org/contracts/universal-router/overview>.

## Security notes

TODO(curator): approval surface, slippage, recipient checks, callbacks, multicall framing.

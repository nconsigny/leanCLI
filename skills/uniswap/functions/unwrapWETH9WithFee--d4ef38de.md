# unwrapWETH9WithFee

**Signature**: `unwrapWETH9WithFee(uint256 amountMinimum,uint256 feeBips,address feeRecipient)`

**Selector**: `0xd4ef38de`

**Mutability**: payable

**Contract**: `SwapRouter02` (Uniswap)

## Inputs
- `amountMinimum` (`uint256`): TODO(curator): describe
- `feeBips` (`uint256`): TODO(curator): describe
- `feeRecipient` (`address`): TODO(curator): describe

## Outputs
- (none)

## What it does

TODO(curator): operational semantics for `SwapRouter02.unwrapWETH9WithFee` — see <https://docs.uniswap.org/contracts/v3/reference/periphery/SwapRouter>.

## Security notes

TODO(curator): approval surface, slippage, recipient checks, callbacks, multicall framing.

# execute

**Signature**: `execute(bytes commands,bytes[] inputs,uint256 deadline)`

**Selector**: `0x3593564c`

**Mutability**: payable

**Contract**: `UniversalRouter` (Uniswap)

## Inputs
- `commands` (`bytes`): TODO(curator): describe
- `inputs` (`bytes[]`): TODO(curator): describe
- `deadline` (`uint256`): TODO(curator): describe

## Outputs
- (none)

## What it does

TODO(curator): operational semantics for `UniversalRouter.execute` — see <https://docs.uniswap.org/contracts/universal-router/overview>.

## Security notes

TODO(curator): approval surface, slippage, recipient checks, callbacks, multicall framing.

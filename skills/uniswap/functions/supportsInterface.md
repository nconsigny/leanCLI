# supportsInterface

**Signature**: `supportsInterface(bytes4 interfaceId)`

**Selector**: `0x01ffc9a7`

**Mutability**: pure

**Contract**: `UniversalRouter` (Uniswap)

## Inputs
- `interfaceId` (`bytes4`): TODO(curator): describe

## Outputs
- `(unnamed)` (`bool`): TODO(curator): describe

## What it does

TODO(curator): operational semantics for `UniversalRouter.supportsInterface` — see <https://docs.uniswap.org/contracts/universal-router/overview>.

## Security notes

TODO(curator): approval surface, slippage, recipient checks, callbacks, multicall framing.

# sweepToken

**Signature**: `sweepToken(address token,uint256 amountMinimum)`

**Selector**: `0xe90a182f`

**Mutability**: payable

**Contract**: `SwapRouter02` (Uniswap)

## Inputs
- `token` (`address`): TODO(curator): describe
- `amountMinimum` (`uint256`): TODO(curator): describe

## Outputs
- (none)

## What it does

TODO(curator): operational semantics for `SwapRouter02.sweepToken` — see <https://docs.uniswap.org/contracts/v3/reference/periphery/SwapRouter>.

## Security notes

TODO(curator): approval surface, slippage, recipient checks, callbacks, multicall framing.

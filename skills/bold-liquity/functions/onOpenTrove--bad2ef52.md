# onOpenTrove

**Signature**: `onOpenTrove(address _owner,uint256 _troveId,TroveChange _troveChange,uint256 _annualInterestRate)`

**Selector**: `0xbad2ef52`

**Mutability**: nonpayable

**Contract**: `TroveManager` (Liquity V2 / BOLD)

## Inputs
- `_owner` (`address`): TODO(curator): describe
- `_troveId` (`uint256`): TODO(curator): describe
- `_troveChange` (`TroveChange`): TODO(curator): describe
- `_annualInterestRate` (`uint256`): TODO(curator): describe

## Outputs
- (none)

## What it does

TODO(curator): operational semantics for `TroveManager.onOpenTrove` — see <https://github.com/liquity/bold/blob/main/README.md>.

## Security notes

TODO(curator): permission boundary, oracle dependency, hint correctness for sorted-list insertion.

# transferFrom

**Signature**: `transferFrom(address from,address to,uint256 amount)`

**Selector**: `0x23b872dd`

**Mutability**: nonpayable

**Contract**: `BoldToken` (Liquity V2 / BOLD)

## Inputs
- `from` (`address`): TODO(curator): describe
- `to` (`address`): TODO(curator): describe
- `amount` (`uint256`): TODO(curator): describe

## Outputs
- `(unnamed)` (`bool`): TODO(curator): describe

## What it does

TODO(curator): operational semantics for `BoldToken.transferFrom` — see <https://github.com/liquity/bold/blob/main/README.md>.

## Security notes

TODO(curator): permission boundary, oracle dependency, hint correctness for sorted-list insertion.

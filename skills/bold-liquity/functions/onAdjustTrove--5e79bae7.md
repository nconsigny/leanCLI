# onAdjustTrove

**Signature**: `onAdjustTrove(uint256 _troveId,uint256 _newColl,uint256 _newDebt,TroveChange _troveChange)`

**Selector**: `0x5e79bae7`

**Mutability**: nonpayable

**Contract**: `TroveManager` (Liquity V2 / BOLD)

## Inputs
- `_troveId` (`uint256`): TODO(curator): describe
- `_newColl` (`uint256`): TODO(curator): describe
- `_newDebt` (`uint256`): TODO(curator): describe
- `_troveChange` (`TroveChange`): TODO(curator): describe

## Outputs
- (none)

## What it does

TODO(curator): operational semantics for `TroveManager.onAdjustTrove` — see <https://github.com/liquity/bold/blob/main/README.md>.

## Security notes

TODO(curator): permission boundary, oracle dependency, hint correctness for sorted-list insertion.

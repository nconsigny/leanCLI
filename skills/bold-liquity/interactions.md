# bold-liquity — interactions

TODO(curator): pre-sign recipes the agent should follow. Each recipe
enumerates the tool calls (`chain_read`, `tx.simulate`, etc.) in order
and the final `propose_send` shape.

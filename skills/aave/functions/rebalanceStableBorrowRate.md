# rebalanceStableBorrowRate

**Signature**: `rebalanceStableBorrowRate(address asset,address user)`

**Selector**: `0xcd112382`

**Mutability**: nonpayable

**Contract**: `Pool` (Aave V3)

## Inputs
- `asset` (`address`): TODO(curator): describe
- `user` (`address`): TODO(curator): describe

## Outputs
- (none)

## What it does

TODO(curator): operational semantics for `Pool.rebalanceStableBorrowRate` — see <https://aave.com/docs/developers/smart-contracts/pool>.

## Security notes

TODO(curator): permission boundary, oracle dependency, slippage / health-factor implication.

# deposit

**Signature**: `deposit(address asset,uint256 amount,address onBehalfOf,uint16 referralCode)`

**Selector**: `0xe8eda9df`

**Mutability**: nonpayable

**Contract**: `Pool` (Aave V3)

## Inputs
- `asset` (`address`): TODO(curator): describe
- `amount` (`uint256`): TODO(curator): describe
- `onBehalfOf` (`address`): TODO(curator): describe
- `referralCode` (`uint16`): TODO(curator): describe

## Outputs
- (none)

## What it does

TODO(curator): operational semantics for `Pool.deposit` — see <https://aave.com/docs/developers/smart-contracts/pool>.

## Security notes

TODO(curator): permission boundary, oracle dependency, slippage / health-factor implication.
